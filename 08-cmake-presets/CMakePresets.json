{
    "version": 2,
    "cmakeMinimumRequired": {
        "major": 3,
        "minor": 20,
        "patch": 0
    },
    "configurePresets": [
        {
            "name": "base",
            "generator": "Unix Makefiles",
            "binaryDir": "${sourceDir}/build-zuzia",
            "hidden": true,
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "Debug"
            }
        },
        {
            "name": "build-debug",
            "generator": "Unix Makefiles",
            "binaryDir": "${sourceDir}/build-debug",
            "inherits": "base"
        }
    ]
}
